<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

    <f:optionalBlock name="on"
                     title="${%Populate a revision for the job}"
                     checked="${instance.on}"
                     help="/plugin/jobrevision/help.html">
        <f:entry field="revision" title="${%Revision}">
            <f:textbox />
        </f:entry>
    </f:optionalBlock>

</j:jelly>